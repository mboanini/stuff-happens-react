export default function Round (roundId, gameId, cardId, roundNumber, result, position, startTime) {
    this.roundId = roundId;
    this.gameId = gameId;
    this.cardId = cardId;
    this.roundNumber = roundNumber;
    this.result = result;
    this.position = position;
    this.startTime = startTime;
}